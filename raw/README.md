# Raw Sources

Store immutable inputs here.

- `sources/` for articles, notes, transcripts, PDFs exported to text/markdown, and similar source material.
- `assets/` for downloaded images and attachments referenced by those sources.

The LLM should read from this directory but not rewrite source contents.
